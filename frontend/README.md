# Frontend

## Run locally

```bash
npm install
npm run dev
```

## Deploy to Vercel

- Import the `frontend` directory as a project in Vercel
- Ensure the project root is set to the `frontend` folder
- Set the environment variable:

```bash
NEXT_PUBLIC_API_URL=https://your-render-api-url.onrender.com/calculate
```
